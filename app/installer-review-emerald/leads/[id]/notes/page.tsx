import { LeadWorkspacePlaceholder } from '@/components/LeadWorkspacePlaceholder';

export default function LeadNotesPage() {
  return <LeadWorkspacePlaceholder eyebrow="Notes" title="Notes workspace not yet available">Existing installer notes remain available in the Overview. The append-only lead-local notes service has not been implemented in this step.</LeadWorkspacePlaceholder>;
}
