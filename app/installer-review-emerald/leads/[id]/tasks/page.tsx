import { LeadWorkspacePlaceholder } from '@/components/LeadWorkspacePlaceholder';

export default function LeadTasksPage() {
  return <LeadWorkspacePlaceholder eyebrow="Tasks" title="Tasks are coming in a later release step">No task records or task counts are available yet. Continue to use the existing follow-up controls in the Overview.</LeadWorkspacePlaceholder>;
}
